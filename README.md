# 🎯 Interview Questions - Blog Task

## 2. TypeScript-এ keyof কিওয়ার্ডের ব্যবহার কী? 
TypeScript-এ কোড লিখতে গেলে আমরা প্রায়ই এমন পরিস্থিতির মুখোমুখি হই যেখানে আমাদের একটি অবজেক্টের প্রোপার্টির নাম (key) গুলোর উপর ভিত্তি করে কাজ করতে হয়। কিন্তু JavaScript-এ এই নামগুলো সাধারণত স্ট্রিং হিসেবে থাকে, যার ফলে ভুল প্রোপার্টি নাম ব্যবহারের ঝুঁকি থেকে যায়। এখানেই TypeScript-এর keyof কীওয়ার্ডটি একটি অসাধারণ ভূমিকা পালন করে।

### keyof কী?
 keyof হলো একটি TypeScript অপারেটর যা একটি টাইপ (Type) নেয় এবং সেই টাইপের সকল পাবলিক প্রোপার্টির নাম এর ইউনিয়ন (Union) টাইপ হিসেবে রিটার্ন করে।

এটাকে একটি "প্রোপার্টি নামের টাইপ" তৈরির যন্ত্র হিসেবেও ভাবা যেতে পারে। এটি আমাদেরকে প্রোপার্টির নামগুলোকে টাইপ-সেফ (Type-Safe) উপায়ে ব্যবহার করতে সাহায্য করে, অর্থাৎ কম্পাইল টাইমেই (Compile Time) ভুল ধরে ফেলে।


**কার্যকারিতা :**
   - **নিরাপদ প্রোপার্টি ব্যবহার:** একটি ফাংশনে বা ভেরিয়েবলে প্রোপার্টির নাম পাস করার সময় যেন শুধুমাত্র বৈধ নামগুলোই ব্যবহার করা যায়, তা নিশ্চিত করা।

  - **জেনেরিক টুল বানানো:** Pick, Omit, বা getProperty-এর মতো জেনেরিক ইউটিলিটি তৈরি করতে এটি মূলে কাজ করে।

  - **ম্যাপড টাইপ তৈরি:** একটি টাইপের সকল প্রোপার্টিকে অন্য টাইপে ম্যাপ (রূপান্তর) করতে সাহায্য করে।

  - **কম্পাইল-টাইমে ভুল ধরা:** রানটাইমে undefined বা Error হওয়ার বদলে, ভুল প্রোপার্টি নাম ব্যবহার করলেই TypeScript কম্পাইল করার সময় Error দেখিয়ে দেয়।

**উদাহরণ :** 

ধরা যাক, একটি User টাইপ আছে

```bash
type User = {
  name: string;
  age: number;
  email: string;
};


type UserKeys = keyof User; // এটি এখন "name" | "age" | "email" টাইপ
```
এখানে, UserKeys টাইপটি শুধুমাত্র "name", "age", অথবা "email" স্ট্রিং তিনটি গ্রহণ করতে পারবে। অন্য কোনো স্ট্রিং (যেমন "address") ব্যবহার করলে TypeScript সাথে সাথে Error দেবে।



```bash
// একটি জেনেরিক ফাংশন যা যেকোনো অবজেক্ট থেকে প্রোপার্টির ভ্যালু নিরাপদে রিটার্ন করে
function getProperty<T, K extends keyof T>(obj: T, key: K): T[K] {
  return obj[key];
}

const user: User = {
  name: "আনিকা",
  age: 28,
  email: "anika@example.com"
};

// সঠিক ব্যবহার
const userName = getProperty(user, "name"); // টাইপ হবে string
const userAge = getProperty(user, "age");   // টাইপ হবে number

// ভুল ব্যবহার (কম্পাইল সময়েই এরর)
const userNickname = getProperty(user, "nickname");
```
T হলো জেনেরিক টাইপ যা যেকোনো অবজেক্ট হতে পারে। `K extends keyof T` দিয়ে TypeScript-কে বলা হয় যে K শুধুমাত্র T অবজেক্টের বৈধ প্রোপার্টি নামই হতে পারবে। যেমন getProperty(user, "name") call করলে T হয় User এবং K হয় "name", আর TypeScript জানে যে রিটার্ন ভ্যালুর টাইপ হবে string। কিন্তু "nickname" দিলেই সাথে সাথে Error দেখায় কারণ User-তে এই নামে কোনো প্রোপার্টি নেই।

### সারসংক্ষেপ
keyof আমাদেরকে অনুমোদিত প্রোপার্টি নামের একটি তালিকা বানাতে এবং ব্যবহার করতে সাহায্য করে, যাতে আমরা ভুল করে অবজেক্টে নেই এমন কোন প্রোপার্টি অ্যাক্সেস করার চেষ্টা না করি। এটি TypeScript-এর টাইপ সিস্টেমকে আরও স্মার্ট এবং আমাদের কোডকে আরও নিরাপদ করে তোলে।


---
## 5. TypeScript-এ ইউনিয়ন (Union) এবং ইন্টারসেকশন (Intersection) টাইপের ব্যবহার।

`TypeScript` তার শক্তিশালী টাইপ সিস্টেমের মাধ্যমে ডেভেলপারদের কোডের নমনীয়তা (Flexibility) এবং নিরাপত্তা (Safety) উভয়ই নিশ্চিত করতে সাহায্য করে। এই টাইপ সিস্টেমের দুটি অপরিহার্য ধারণা হলো `ইউনিয়ন` এবং `ইন্টারসেকশন` টাইপস। এই টাইপগুলো জটিল ডেটা কাঠামো তৈরি করতে এবং বিভিন্ন ধরনের ডেটা দক্ষতার সাথে পরিচালনা করতে ব্যবহৃত হয়। 

এই দুটি টাইপের ব্যবহার এবং তাদের মধ্যেকার পার্থক্য উদাহরণসহ বিশ্লেষণ করব।

### ১. ইউনিয়ন টাইপ (|): 
ইউনিয়ন টাইপ হল TypeScript-এর একটি বিশেষ ফিচার যা প্রোগ্রামারকে একটি ভেরিয়েবলে একাধিক টাইপের ডাটা সংরক্ষণের সুযোগ দেয়। মূলত এটি OR লজিক অনুসরণ করে - অর্থাৎ একটি ভেরিয়েবল টাইপ A অথবা টাইপ B অথবা টাইপ C এর যেকোনো একটি হতে পারবে। TypeScript-এ এটি পাইপ চিহ্ন (|) ব্যবহার করে নির্দেশ করা হয়।


**কার্যকারিতা :**
   - এই টাইপটি বিশেষভাবে কার্যকরী যখন আমরা নিশ্চিতভাবে জানি না যে কোন নির্দিষ্ট টাইপের ডাটা আসবে, কিন্তু সীমিত সংখ্যক টাইপের মধ্যে একটি হবে।

  - Union Type ব্যবহারের মাধ্যমে আমরা রানটাইম এরর এড়াতে সক্ষম হই এবং কোডের টাইপ সেফটি নিশ্চিত করতে পারি। 
  - TypeScript কম্পাইল টাইমে স্বয়ংক্রিয়ভাবে চেক করে যে শুধুমাত্র ডিফাইন করা টাইপগুলোই ব্যবহার করা হচ্ছে, যা প্রোগ্রামের নিরাপত্তা বৃদ্ধি করে।
  - এটি এমন ফাংশন বা ভ্যারিয়েবল লেখার জন্য খুবই উপযোগী যা বিভিন্ন ধরনের ইনপুট বা আউটপুট গ্রহণ করতে পারে।


**উদাহরণ :** 

ধরা যাক, একটি ফাংশন আইডি গ্রহণ করে যা সংখ্যা (number) অথবা স্ট্রিং (string) যেকোনো একটি হতে পারে।

```bash
type Id = number | string;

function printId(id: Id) {
    console.log(`The ID is: ${id}`);
    
    // টাইপ ন্যারোয়িং (Type Narrowing) প্রয়োজন
    if (typeof id === "string") {
        console.log(`ID in uppercase: ${id.toUpperCase()}`);
    }
}

printId(101); // আউটপুট: The ID is: 101
printId("TS-404"); // আউটপুট: The ID is: TS-404

```
এখানে, Id ভ্যারিয়েবলটি হয় একটি সংখ্যা, না হয় একটি স্ট্রিং হবে। ফাংশনের মধ্যে আমরা অবশ্যই `typeof` ব্যবহার করে টাইপ ন্যারোয়িং করব যাতে সেই নির্দিষ্ট টাইপের জন্য উপযুক্ত মেথডগুলি (যেমন, toUpperCase()) নিরাপদে ব্যবহার করা যায়।

### ২. ইন্টারসেকশন টাইপ (&): 

ইন্টারসেকশন টাইপ হল TypeScript-এর একটি অত্যন্ত শক্তিশালী ফিচার যা একাধিক টাইপের সকল প্রোপার্টি এবং মেথড একত্রিত করে একটি নতুন টাইপ তৈরি করে। এটি AND লজিক অনুসরণ করে - অর্থাৎ ফলাফলস্বরূপ টাইপটিতে অবশ্যই টাইপ A এবং টাইপ B এবং টাইপ C এর সমস্ত প্রোপার্টি উপস্থিত থাকবে। Intersection Type ডিক্লেয়ার করতে আমরা & (অ্যাম্পারস্যান্ড) সিম্বল ব্যবহার করি, যা বিভিন্ন টাইপকে পরস্পরের সাথে সংযুক্ত করে। 


**কার্যকারিতা :**

- এই টাইপটি বিশেষভাবে উপযোগী যখন আমাদের বিভিন্ন উৎস থেকে প্রোপার্টি সংগ্রহ করে একটি সম্পূর্ণ অবজেক্ট তৈরি করতে হয়

- যখন একাধিক ইন্টারফেসের প্রয়োজনীয়তা একই অবজেক্টে বাস্তবায়ন করতে হয়।

- যখন আমরা ইনহেরিটেন্স ইউজ না করে বেস টাইপ এক্সটেন্ড করতে চাই। এটি আমাদেরকে কম্পোজিশন ওভার ইনহেরিটেন্স প্রিন্সিপাল ফলো করতে সাহায্য করে।

- Intersection Type আমাদেরকে আরও সমৃদ্ধ এবং নির্দিষ্ট টাইপ ডেফিনিশন তৈরি করতে সাহায্য করে, যা কোডের পাঠযোগ্যতা এবং পুনর্ব্যবহারযোগ্যতা বৃদ্ধি করে এবং টাইপ সেফটি বজায় রাখে।




**উদাহরণ :** 

ধরা যাক, আমাদের কাছে একজন কর্মচারী (Employee) এবং একজন প্রশাসক (Administrator)-এর জন্য দুটি মৌলিক টাইপ রয়েছে। আমরা একজন সুপারভাইজার (Supervisor) তৈরি করতে চাই যার এই দুটি টাইপেরই বৈশিষ্ট্য থাকবে।

```bash
// ১. Employee টাইপ
type Employee = {
    employeeId: number;
    name: string;
    startDate: Date;
};

// ২. Administrator টাইপ 
type Administrator = {
    adminLevel: "Level1" | "Level2";
    permissions: string[];
};

// ৩. Supervisor টাইপ তৈরি করতে ইন্টারসেকশন ব্যবহার
type Supervisor = Employee & Administrator;

const newSupervisor: Supervisor = {
    employeeId: 500,
    name: "রহিম খান",
    startDate: new Date(),
    adminLevel: "Level2",
    permissions: ["read", "write", "manage"],
};

console.log(newSupervisor.name); // Employee টাইপের বৈশিষ্ট্য
console.log(newSupervisor.permissions); // Administrator টাইপের বৈশিষ্ট্য

```

এখানে, `Supervisor` টাইপটি Employee এবং Administrator উভয়ের সমস্ত বৈশিষ্ট্যকে একত্রিত করে তৈরি হয়েছে। একটি Supervisor অবজেক্ট তৈরি করতে, এই সমস্ত বৈশিষ্ট্যগুলিই সরবরাহ করা বাধ্যতামূলক।


---                                       
### কখন কোন টাইপ ব্যবহার করবেন?
- `Union Type` ব্যবহার করুন যখন একটি ভেরিয়েবল বা প্যারামিটার একাধিক টাইপের যেকোন একটি হতে পারে।

- `Intersection Type` ব্যবহার করুন যখন একটি অবজেক্টকে একই সাথে একাধিক টাইপের সকল প্রোপার্টি ধারণ করতে হবে।


### সারসংক্ষেপ
`ইউনিয়ন টাইপ (|)` কোডে নমনীয়তা নিয়ে আসে, যা বিভিন্ন ধরনের ইনপুট বা আউটপুট দক্ষতার সাথে সামলাতে সাহায্য করে। অন্যদিকে, `ইন্টারসেকশন টাইপ (&)` ছোট ছোট অংশ থেকে নতুন, সমৃদ্ধ অবজেক্ট টাইপ তৈরি করতে সাহায্য করে, যা টাইপ কম্পোজিশনের মূল ভিত্তি। এই দুটি কৌশল আয়ত্ত করা TypeScript-এ পরিচ্ছন্ন, নিরাপদ এবং শক্তিশালী কোড লেখার জন্য অত্যাবশ্যক।